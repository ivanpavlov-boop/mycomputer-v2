# Tests

The frontend package includes a placeholder `npm run test` command using Vitest.

Add component and page tests after dependencies are installed with:

```bash
cd frontend
npm install
npm run test
```
